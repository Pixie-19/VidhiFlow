# Tambo – required for chat/Generative UI
NEXT_PUBLIC_TAMBO_API_KEY=

# App URL (for MCP server and server-side tool calls)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Bhashini – optional; without it, translation returns mock prefixed text
BHASHINI_API_KEY=
BHASHINI_API_BASE=https://api.bhashini.gov.in

# NJDG / MoSPI – optional; without them, mock data is returned
NJDG_API_BASE=
MOSPI_API_BASE=

# MCP server (when running as separate process)
VIDHIFLOW_APP_URL=http://localhost:3000

# Generative Bench: Mock NJDG Server (MCP at http://localhost:8261/mcp)
# Optional; get_case_details tool falls back to mock data if unreachable.
